# Copy this file to `.env.local` (ignored) and adjust as needed.

VITE_APP_ID=deep-persona-mob
VITE_NPC_PROFILE=mob

# Frontend -> backend gateway
# - Keep `/npc-api` for local dev (Vite proxies to http://localhost:4000)
# - Point to your deployed Fastify backend in other environments.
VITE_NPC_API_BASE_URL=/npc-api
VITE_NPC_API_KEY=replace-me          # matches backend NPC_GATEWAY_KEY
