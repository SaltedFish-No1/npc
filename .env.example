# Copy this file to `.env.local` and fill in your values.
# When running `pnpm dev`, requests to /ark-api are proxied to Volcengine,
# so using the same relative path avoids browser-side CORS errors.

VITE_APP_ID=deep-persona-mob
VITE_API_BASE_URL=/ark-api

# Optional: Select which NPC character to use (default: mob)
# Available options: mob, reigen, tech-architect
# VITE_NPC_PROFILE=mob
