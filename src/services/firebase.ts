/**
 * Firebase support has been removed. This placeholder module exists only so that
 * older imports (if any) fail loudly during development instead of pulling in a
 * heavy dependency. Remove any lingering references to '@/services/firebase'.
 */

throw new Error('Firebase has been removed from this project.');
